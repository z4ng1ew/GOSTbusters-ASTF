<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ✅ ПРАВИЛЬНО: Корректное наследование от родительского POM -->
    <parent>
        <groupId>org.owasp</groupId>
        <artifactId>api-security-testing-framework</artifactId>
        <version>1.0-SNAPSHOT</version>
        <!-- ✅ УКАЗАНО: относительный путь к родителю -->
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- ✅ ПРАВИЛЬНО: artifactId соответствует имени папки -->
    <artifactId>plugin-api</artifactId>
    <packaging>jar</packaging>
    <name>OWASP ASTF Plugin API</name>
    <description>API for developing plugins for the ASTF framework</description>

    <properties>
        <!-- ✅ ИСПОЛЬЗУЕМ НАСЛЕДУЕМЫЕ СВОЙСТВА ОТ РОДИТЕЛЯ -->
        <!-- Никаких собственных версий jackson, log4j - только через родителя -->
    </properties>

    <dependencies>
        <!-- ✅ Jackson для JSON/YAML (версии из родителя) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <!-- ✅ ВЕРСИЯ НЕ УКАЗАНА - БУДЕТ ВЗЯТА ИЗ РОДИТЕЛЯ -->
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
            <!-- ✅ ВЕРСИЯ НЕ УКАЗАНА - БУДЕТ ВЗЯТА ИЗ РОДИТЕЛЯ -->
        </dependency>

        <!-- ✅ Логирование (версия из родителя) -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <!-- ✅ ВЕРСИЯ НЕ УКАЗАНА - БУДЕТ ВЗЯТА ИЗ РОДИТЕЛЯ -->
        </dependency>

        <!-- ✅ HTTP клиент (версия указана явно, т.к. нет в родителе) -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version> <!-- ✅ ЯВНО УКАЗАНА ВЕРСИЯ -->
        </dependency>

        <!-- ✅ ЗАВИСИМОСТЬ ОТ ОСНОВНОГО МОДУЛЯ (без classifier) -->
        <dependency>
            <groupId>org.owasp</groupId>
            <artifactId>api-security-testing-framework</artifactId>
            <version>1.0-SNAPSHOT</version>
            <!-- УБРАНО: <classifier>core</classifier> -->
            <scope>provided</scope>
        </dependency>

        <!-- ✅ Тесты (версии из родителя) -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                </configuration>
            </plugin>

            <!-- ✅ Генерация sources JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- ✅ Генерация javadoc JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- ✅ УПРАВЛЕНИЕ УПАКОВКОЙ (убран shade-plugin - он не нужен для API) -->
            <!-- shade-plugin используется в основном JAR, не в API -->
        </plugins>
    </build>
</project>